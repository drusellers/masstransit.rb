require 'spec'
require "specs/EndpointFactory.rb"

describe "Passed an endpoint URI" do
  it "should have two supported endpoint factories" do
    EndpointFactory.factories.should have(2).things
  end

  it "should handle loopback addresses" do
    EndpointFactory.factories.should include :loopback
  end

  it "should handle rabbitmq addresses" do
    EndpointFactory.factories.should include :loopback
  end
  
end


