require 'spec'
require "lib/masstransit"

describe "New subscription" do

  before(:each) do
    @messageType = "mt"
  end

	it "should return the message type provided" do
		subscription = MassTransit::Subscription.new lambda { |msg| 
      puts msg 
    }
	end

end
